{
  "name": "subscriber-cloud",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build --base=/subscribers-cloud/",
    "preview": "vite preview",
    "deploy": "cp -r dist/* ../techydrrroid-ktor/src/main/resources/static/"
  },
  "devDependencies": {
    "vite": "^3.0.7"
  },
  "dependencies": {
    "TagCloud": "^2.2.0"
  }
}
