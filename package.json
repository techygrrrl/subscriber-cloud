{
  "name": "subscriber-cloud",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build",
    "build:techydrrroid": "rm -rf dist && vite build --base=/subscribers-cloud/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.184",
    "vite": "^3.0.7"
  },
  "dependencies": {
    "TagCloud": "^2.2.0",
    "lodash": "^4.17.21"
  }
}
